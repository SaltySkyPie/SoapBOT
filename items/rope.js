module.exports = {
    name: 'rope',
    async execute(message, args, BotClient, functions, user) {
       message.reply("You used **Rope**! Next time someone tries to drop your soap they'll automatically fail.")
    }
}