module.exports = {
    name: 'soap hardener',
    async execute(message, args, BotClient, functions, user) {
       message.reply("You used **Soap Hardener**! Next time someone tries to steal from you, they'll automatically fail.")
    }
}